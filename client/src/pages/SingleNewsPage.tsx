import { JSX } from 'react';
import { Flex } from '@chakra-ui/react';
import { BreadcrumbSection } from '@/components/BreadcrumbSection';

export const SingleNewsPage = (): JSX.Element => (
    <Flex direction='column'>
        <BreadcrumbSection bg='' />
    </Flex>
);
